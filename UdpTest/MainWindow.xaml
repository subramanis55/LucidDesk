<Window x:Class="UdpTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UdpTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        <RichTextBox Grid.Column="0" FontSize="15" Name="outputTextBox">

        </RichTextBox>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions >
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
            <RichTextBox Grid.Row="0" FontSize="15" Name="ClientTextBox">
                
            </RichTextBox>
            <Border Grid.Row="1" >
                <Button Content="send"  Click="send_Click" Height="40" Width="120"/>
            </Border>
        </Grid>
    </Grid>
        
</Window>
